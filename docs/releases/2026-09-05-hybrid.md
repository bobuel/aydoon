# Hybrid production release — September 5, 2026

User selected the Hybrid localhost option and explicitly requested publication.

## Baseline and rollback

- Production before this release: `ab797b279701cb1b5ac2f9d8fd03f2f7fd298cd8`.
- Successful previous Pages run: `33969143281`.
- Previous homepage assets: `index-DwkZtHl_.js` and `index-YKhdBRIU.css`.
- Baseline homepage: HTTP 200 from GitHub Pages; ETag `"6a9c19d1-b46"`.
- Rollback: revert this release's merge commit on `main` and let the existing Pages workflow redeploy. Never force-push or change DNS.

## Scope

- Promotes only the selected Hybrid design, without the local comparison bar or alternate designs.
- Light persistent identity panel, compact typography, flat rows, Work / Builds / Games navigation.
- Keeps complete professional case-study evidence, the About narrative, contact links, legacy Work route, and shared case-list anchors.
- Adds the Brassline entry already present in the selected preview; shortens project summaries to match that preview.
- Preserves the résumé file without adding a résumé link, the social image, custom-domain metadata, and static architecture.
- `App.tsx` now renders `HybridPortfolio`; `index.tsx` imports `hybrid.css`. The older components and stylesheet are retained as reference but are not in the production entry point.

## Verification

Required before publication: `npm run check`, `npm run build:pages:production`, `npm run scan:secrets`. The Hybrid suite exercises the actual app entry, view switching, profile and project links, all case-study content, Games, About, Work redirect, 404 recovery, focus, shared anchors, metadata transitions, and automated accessibility.

After Pages succeeds, verify the homepage, About, Builds, Games, Work, all three cases, PDF, robots, sitemap, and deployed assets. Browser visual QA is not claimed as part of this release; the user selected the interactive local prototype.
